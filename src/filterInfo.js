
/**
 * 
 * @param {*} info 
 */
module.exports = function filterAndFinaliseInfo( info, filter ){

    console.log( 'Filter And Finalise' );

    /**
     * Finalise the info object.
     */
    
    

     /**
     * Search for circular dependencies as these files 
     * need splitting in two when transformed.
     */

    return info;

}